#include "Wrapper.h"

// In main i Include Wrapper.h so i can create a FitnessAppWrapper object. After i create that object I call runApp() which launches my code.

void main(void)
{
	FitnessAppWrapper project;
	project.runApp();
}